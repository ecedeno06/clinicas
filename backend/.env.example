# Copiar como .env y completar con los datos reales (no se sube a git)

DATABASE_URL=postgresql://usuario:password@192.168.0.17:5432/clinica_medica
DB_SSL=false
# Nombre de la base de datos mostrado en el header del dashboard (para
# saber a simple vista a que entorno esta conectado el backend). Se toma
# directo del nombre de la base en DATABASE_URL (ej. "clinica-dev",
# "clinica" en Neon) -- normalmente no hace falta tocar esto.
# DB_INSTANCE_NAME es un override opcional, por si se quiere mostrar un
# nombre distinto al de la base real.
DB_INSTANCE_NAME=

PORT=3001
JWT_SECRET=cambia-este-secreto
# Vida del access token (corto a proposito -- ver refresh token abajo).
JWT_EXPIRES_IN=30m
CORS_ORIGIN=http://localhost:4201

# Refresh token: opaco, guardado en la tabla "sesiones", rotado en cada uso
# (POST /auth/refresh). Es el limite duro de una sesion aunque el usuario
# siga activo -- independiente de SESSION_INACTIVITY_LIMIT_MINUTES, que es
# solo un control de UX del lado del cliente.
REFRESH_TOKEN_EXPIRES_IN_HOURS=12

# API key de Google restringida a "Geocoding API" unicamente (Google Cloud
# Console -> Credentials). Nunca se expone al frontend -- solo la usa
# geocodificacion.controller.js para traer provincia/distrito de una
# direccion de paciente. Ver DISENO-GEOCODIFICACION-INVERSA.md.
MAPKEY=

# 2FA (TOTP), pista de contrasena y logout por inactividad -- ver
# DISENO-AUTENTICACION-2FA-SESION.md.
# Clave para cifrar usuarios.two_factor_secret (AES-256-CBC): exactamente
# 32 caracteres, o 64 caracteres hexadecimales. Generar con, por ejemplo:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRYPTO_SECRET_KEY=
# % maximo de similitud (Levenshtein) permitido entre la pista y la nueva
# contrasena al guardarla.
PASSWORD_HINT_MAX_SIMILARITY=70
# Minutos de inactividad antes de cerrar la sesion automaticamente, y
# minutos antes de ese cierre en que se muestra el aviso.
SESSION_INACTIVITY_LIMIT_MINUTES=15
SESSION_WARNING_BEFORE_MINUTES=2
# Cada cuantos minutos el frontend renueva el access token en segundo
# plano mientras el usuario sigue activo (debe ser menor que JWT_EXPIRES_IN).
SESSION_REFRESH_INTERVAL_MINUTES=10

# Envio de correo (backend/src/utils/correo.js). Hoy: cuenta de Gmail con
# verificacion en dos pasos + contrasena de aplicacion, mientras se define
# un dominio propio -- ver Guia_Gmail_2FA_AppPassword.docx. SMTP_PASS es
# el codigo de 16 caracteres de la contrasena de aplicacion (NO la
# contrasena normal de la cuenta).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
# Remitente que ve el destinatario (opcional -- si se omite, usa SMTP_USER).
SMTP_FROM=

# Al arrancar, si no existe ningun usuario con el email de SUPER_ADMIN_USER
# lo crea como super administrador (bootstrap del primer super admin de una
# instalacion nueva). SUPER_ADMIN_PASS debe ser ya un HASH bcrypt, no la
# contrasena en texto plano -- generarlo con:
#   node src/utils/generarPasswordHash.js "MiContrasena"
# Si SUPER_ADMIN_PASS se deja vacio, no crea nada. Si el usuario ya existe,
# tampoco lo toca (no resetea su contrasena en cada reinicio).
SUPER_ADMIN_USER=admin@clinica
SUPER_ADMIN_PASS=
